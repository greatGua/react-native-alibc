// This files provides compatibility without tree platform.
import { requireNativeComponent } from "react-native";

const  WebView  = requireNativeComponent("RNAliBCWebView");

export default WebView;
